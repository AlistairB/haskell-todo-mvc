version: '3'

services:
  dev:
    build:
      dockerfile: support/dev/Dockerfile
      context: .
    working_dir: /work
    command: support/dev/run
    volumes:
      - .:/work
      - yarn-cache:/tmp/yarn-cache

volumes:
  yarn-cache:
    external: true
